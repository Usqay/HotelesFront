export interface StoreHouseMovementType {
    active ? : boolean
    name ? : string
    id ? : number
    in_out ? : boolean
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
}
