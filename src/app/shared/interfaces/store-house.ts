export interface StoreHouse {
    active ? : boolean
    name ? : string
    description ? : string
    address ? : string,
    is_base ? : boolean
    id ? : number
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
}
