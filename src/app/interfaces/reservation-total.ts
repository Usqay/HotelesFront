export interface ReservationTotal {
    active ? : boolean
    id ? : number
    reservation_id ? : number
    currency_id ? : number
    total ? : number
    total_by ? : number
    discount ? : number
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
}
