export interface CashRegister {
    active ? : boolean
    name ? : string
    location ? : string
    description ? : string
    is_base ? : boolean
    id ? : number
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
}
