export interface TurnChange {
    active ? : boolean
    id ? : number
    turn_id ? : number
    open_by_user_id ? : number
    close_by_user_id ? : number
    currency_base_id ? : number
    open_date ? : Date
    close_date ? : Date
    status_active ? : boolean
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
}
