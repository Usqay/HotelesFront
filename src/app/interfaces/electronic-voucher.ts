export interface ElectronicVoucher {
    active ? : boolean
    date_emitted ? : Date
    electronic_voucher_type_id ? : number
    number ? : number
    serie ? : number
    print ? : boolean
    api_body ? : string
    api_response ? : string
    api_state ? : boolean
    adittional_info ? : string
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
}
