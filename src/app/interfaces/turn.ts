export interface Turn {
    active ? : boolean
    name ? : string
    open_time ? : string
    close_time ? : string
    id ? : number
    created_at ? : Date
    updated_at ? : Date
    deleted_at ? : Date
    hoverState ? : boolean
}
